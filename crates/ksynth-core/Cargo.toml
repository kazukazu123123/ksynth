[package]
name = "ksynth-core"
version.workspace = true
edition.workspace = true

[dependencies]
rayon = "1.11.0"

[build-dependencies]
gix = { version = "0.73.0", default-features = false, features = ["revision", "status"] }
